# 🚀 دليل رفع الملفات إلى الخادم المباشر

## ⚠️ المشكلة الحالية:
الملفات المحلية على جهازك **صحيحة** ✅، لكن الخادم المباشر (https://wesad.kesug.com/) لا يزال يحتوي على الملفات القديمة ❌

---

## 📋 الملفات التي يجب رفعها:

### ✅ ملفات **إلزامية** (يجب رفعها):
1. **`.htaccess`** - يحتوي على CSP المُحدّث
2. **`manifest.json`** - تم إصلاح خطأ Syntax
3. **`script.js`** - يحتوي على دالة `safeBtoa()` المُحدّثة
4. **`index.html`** - يحتوي على CSP meta tag

### 📄 ملفات **اختيارية** (للمعلومات فقط):
5. `FIXES_REPORT.md` - تقرير الإصلاحات
6. `test-fixes.html` - صفحة الاختبار

---

## 🔧 طرق رفع الملفات:

### **الطريقة 1: استخدام FTP/SFTP** (الأكثر شيوعاً)

#### الخطوات:
1. **افتح برنامج FTP** (مثل FileZilla أو WinSCP)
2. **اتصل بالخادم** باستخدام بيانات الاعتماد الخاصة بك:
   - Host: `kesug.com` أو `ftp.kesug.com`
   - Username: اسم المستخدم الخاص بك
   - Password: كلمة المرور
   - Port: `21` (FTP) أو `22` (SFTP)

3. **انتقل إلى مجلد الموقع** (عادةً):
   - `/public_html/` أو
   - `/www/` أو
   - `/htdocs/`

4. **ارفع الملفات التالية** (استبدل القديمة):
   ```
   ✓ .htaccess
   ✓ manifest.json
   ✓ script.js
   ✓ index.html
   ```

5. **تأكد من الرفع الكامل** (انتظر حتى تكتمل جميع الملفات)

---

### **الطريقة 2: استخدام لوحة التحكم (cPanel)**

#### الخطوات:
1. **سجّل الدخول إلى cPanel**:
   - عادةً: `https://kesug.com/cpanel` أو
   - `https://kesug.com:2083`

2. **افتح File Manager** (مدير الملفات)

3. **انتقل إلى مجلد الموقع**:
   - عادةً: `public_html/`

4. **احذف الملفات القديمة** (اختياري لكن مُوصى به):
   - `.htaccess` (القديم)
   - `manifest.json` (القديم)
   - `script.js` (القديم)
   - `index.html` (القديم)

5. **ارفع الملفات الجديدة**:
   - اضغط على زر **Upload**
   - اختر الملفات من:
     ```
     c:\Users\lenovo\Desktop\Wesaad\
     ```
   - ارفع: `.htaccess`, `manifest.json`, `script.js`, `index.html`

6. **تحقق من الأذونات** (Permissions):
   - `.htaccess` → `644`
   - `manifest.json` → `644`
   - `script.js` → `644`
   - `index.html` → `644`

---

### **الطريقة 3: استخدام Git (إذا كان الخادم يدعمه)**

#### الخطوات:
1. **اتصل بالخادم عبر SSH**:
   ```bash
   ssh username@kesug.com
   ```

2. **انتقل إلى مجلد الموقع**:
   ```bash
   cd /path/to/website
   ```

3. **سحب التحديثات من GitHub**:
   ```bash
   git pull origin main
   ```

4. **تحقق من التحديثات**:
   ```bash
   ls -la
   ```

---

## ✅ التحقق من نجاح الرفع:

### **الخطوة 1: مسح الكاش**
بعد رفع الملفات، امسح الكاش:

1. **كاش المتصفح:**
   - اضغط `Ctrl + Shift + Delete`
   - احذف Cached images and files
   - أو افتح الموقع في وضع Incognito: `Ctrl + Shift + N`

2. **كاش الخادم** (إذا كان موجود):
   - في cPanel → ابحث عن "Cache Manager" أو "Clear Cache"
   - أو استخدم Cloudflare → Purge Cache (إذا كنت تستخدمه)

### **الخطوة 2: اختبر الموقع**
1. افتح: `https://wesad.kesug.com/`
2. اضغط `F12` لفتح DevTools
3. افتح تبويب **Console**
4. **تحقق من عدم وجود أخطاء:**
   - ✅ لا توجد أخطاء CSP
   - ✅ لا توجد أخطاء InvalidCharacterError
   - ✅ لا توجد أخطاء Manifest

### **الخطوة 3: اختبر صفحة الاختبار**
1. افتح: `https://wesad.kesug.com/test-fixes.html`
2. يجب أن ترى جميع الاختبارات باللون الأخضر ✅

---

## 🔍 استكشاف الأخطاء:

### **المشكلة: لا تزال الأخطاء موجودة بعد الرفع**

#### **الحل 1: امسح الكاش**
```
المتصفح → Settings → Privacy → Clear browsing data
أو
Ctrl + Shift + Delete
```

#### **الحل 2: تحقق من رفع الملفات**
- تأكد من أن الملفات **استُبدلت** وليست **أُضيفت**
- تحقق من تاريخ التعديل في File Manager

#### **الحل 3: تحقق من الأذونات**
- الملفات يجب أن تكون قابلة للقراءة: `644`
- المجلدات يجب أن تكون: `755`

#### **الحل 4: تحقق من `.htaccess`**
- تأكد من أن الخادم يدعم `.htaccess`
- تأكد من أن `mod_headers` مُفعّل

---

## 📞 إذا واجهت مشاكل:

### **تحقق من:**
1. ✅ هل رُفعت الملفات بنجاح؟
2. ✅ هل الأذونات صحيحة؟
3. ✅ هل مسحت الكاش؟
4. ✅ هل الخادم يدعم `.htaccess`؟

### **إذا استمرت المشكلة:**
- أرسل لي لقطة شاشة من Console
- أرسل لي لقطة شاشة من File Manager (لإظهار الملفات المرفوعة)

---

## 🎯 ملخص سريع:

```bash
# الملفات التي يجب رفعها:
✓ .htaccess
✓ manifest.json  
✓ script.js
✓ index.html

# بعد الرفع:
1. امسح كاش المتصفح (Ctrl + Shift + Delete)
2. افتح الموقع في Incognito (Ctrl + Shift + N)
3. اضغط F12 → تحقق من Console
4. يجب ألا ترى أي أخطاء! ✅
```

---

**ملاحظة مهمة:** الملفات المحلية على جهازك **صحيحة 100%** ✅. المشكلة فقط في أن الخادم المباشر لا يزال يحتوي على الملفات القديمة. بمجرد رفع الملفات الجديدة، ستختفي جميع الأخطاء! 🚀
